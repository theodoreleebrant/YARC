const CHIP8_WIDTH: usize = 64;
const CHIP8_HEIGHT: usize = 32;
const CHIP8_RAM: usize = 4096;

fn main() {
    // legacy
    // let cpu = CPU::new();
    // cpu.load_program(vec![0x13, 0xC5]); // Loads a program which tells it to do 13C5 - see opcode 1NNN
}

